import React from "react";

const AlreadyConnected = (props) => {
  return (
    <div className="alreadyConnected">
      Kite-Chat is open in another window. Click "Open Here" to use here.
    </div>
  );
};

export default AlreadyConnected;
